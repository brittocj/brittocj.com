name = "brittocj-com"
compatibility_date = "2024-08-01"
pages_build_output_dir = "."

# Create a KV namespace in Cloudflare, then replace the id below.
# Dashboard: Workers & Pages > KV > Create namespace (e.g. brittocj-blog-views)
[[kv_namespaces]]
binding = "VIEWS"
id = "REPLACE_WITH_YOUR_KV_NAMESPACE_ID"
